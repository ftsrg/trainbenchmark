package hu.bme.mit.trainbenchmark.railway

query "RouteSensor check"
    route : Route()
    switchPosition : SwitchPosition( ) from route.follows
    switch : Switch( this == switchPosition.switch )
    sensor : Sensor( ) from switch.sensor
    not Sensor(this == sensor, this memberOf route.definedBy)
end
