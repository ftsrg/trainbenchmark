PREFIX  base: <http://www.semanticweb.org/ontologies/2015/trainbenchmark#>
PREFIX  rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT  ?x (COUNT(?o) AS ?outdegree) (COUNT(?s) AS ?indegree)
WHERE
  {   { ?x ?p ?o}
    UNION
      { ?s ?p2 ?x}
  }
GROUP BY ?x
