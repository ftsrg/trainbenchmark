package hu.bme.mit.trainbenchmark.railway

query "SwitchSet check"
    route : Route( )
    Semaphore( this == route.entry, signal == Signal.GO )
    switchPosition : SwitchPosition( this memberOf route.follows, $asd : switch )
    Switch( this == switchPosition.switch, currentPosition.value != switchPosition.position.value )
end
