apply plugin: 'com.github.johnrengelman.shadow'

shadowJar {
	classifier = 'fat'
	manifest { attributes 'Main-Class': 'hu.bme.mit.trainbenchmark.benchmark.orientdb.OrientDbGraphBenchmarkMain' }
}

dependencies {
	compile project(':trainbenchmark-tool-tinkergraph')
	compile group: 'com.orientechnologies', name: 'orientdb-gremlin', version: orientDbVersion
}
